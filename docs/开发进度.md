# 开发进度

## 2026-06-17 MVP 初版

### 已完成

- 项目骨架
  - `package.json`
  - Node 内置 HTTP 服务
  - 原生前端单页应用
  - 静态资源目录

- M1 基础框架
  - 顶部导航
  - 左侧安全引擎菜单
  - 安全事件 / 安全事件模型路由
  - 统一面板、表格、按钮、表单样式

- M2 攻击场景与字典
  - 攻击场景 Mock 数据
  - 严重等级字典
  - 模型状态字典
  - 模型来源字典
  - 攻击结果字典

- M3 安全事件模型列表
  - 模型分类
  - 模型列表
  - 状态展示
  - 启用 / 停用
  - 详情 / 编辑入口

- M4 安全事件模型表单
  - 新建模型
  - 编辑模型
  - 入口告警添加 / 删除
  - DSL 编辑
  - DSL 基础校验
  - 激活状态
  - 通知对象

- M5 模型详情与历史
  - 模型基础信息
  - 入口告警
  - DSL 只读展示
  - 模型历史时间线

- M6 HQL 日志查询
  - 日志查询弹窗
  - HQL 输入
  - 字段模板面板
  - 查询结果表
  - 日志勾选

- M7 安全事件列表
  - 事件列表
  - 统计卡片
  - 标题 / 编号筛选
  - 严重等级筛选
  - 详情 / 攻击图谱入口

- M9 创建安全事件
  - 第一步选择日志 / 事件
  - 第二步填写事件详情
  - 必填校验
  - 创建后写入安全事件 Mock 数据
  - 创建后跳转事件详情告警 Tab

- M10 事件详情基础框架
  - 顶部基础信息
  - Tab 切换
  - 概览 / 告警 / 威胁图谱 / 相关证据 / 影响面入口

- M12 告警模块
  - 告警表格
  - 核心告警 / 入口告警标识
  - 关联条件展示

### 已提供 API

- `GET /api/security/dictionaries`
- `GET /api/security/attack-scenes/tree`
- `GET /api/security/models`
- `POST /api/security/models`
- `GET /api/security/models/{id}`
- `PUT /api/security/models/{id}`
- `DELETE /api/security/models/{id}`
- `POST /api/security/models/{id}/enable`
- `POST /api/security/models/{id}/disable`
- `GET /api/security/models/{id}/history`
- `POST /api/security/models/validate`
- `GET /api/security/incidents`
- `POST /api/security/incidents`
- `GET /api/security/incidents/{id}`
- `GET /api/security/incidents/{id}/overview`
- `GET /api/security/incidents/{id}/alerts`
- `GET /api/security/incidents/{id}/graph`
- `GET /api/security/incidents/{id}/evidence`
- `GET /api/security/incidents/{id}/impact`
- `POST /api/security/logs/search`
- `GET /api/security/logs/templates`
- `GET /api/security/incidents/table-fields`

### 当前占位 / 待完善

- 表格字段配置仅提示，尚未做持久化。
- 威胁图谱当前展示节点/边数量，复杂交互第二阶段实现。
- 概览、证据、影响面已有数据结构和基础展示，字段细节后续补齐。
- 导入 / 导出按钮为占位。
- 添加日志 / 告警按钮为占位。
- 数据为内存 Mock，服务重启后恢复默认数据。
- 尚未接入 PostgreSQL、Elasticsearch、Kafka、Redis、Flink。

### 本地启动

```bash
npm run dev
```

默认地址：

```text
http://127.0.0.1:4173
```
